<header class="navbar navbar-fixed-top navbar-inver">
  <div class="navbar-inner">
    <nav>
      <ul class="nav">
        <li class="logo"><%= link_to image_tag('bike_silhouette.png', width: 80), root_url %></li>
        <li><%= link_to "users", users_url %></li>
        <li><%= link_to "rides", rides_url %></li>
        <% if @current_user %>
          <li><%= link_to "log out", sessions_url, method: :delete %></li>
        <% else %>
          <li><%= link_to "log in", new_sessions_url %></li>
        <% end %>


      </ul>
    </nav>
  </div>
</header>
